

type AccountType {
    _id: ID
    idUser: ID
}
# Account Type
input IAccount {
    _id: ID
    idUser: ID
}
type Query {
 sayHello: String
}

# Mutations
type Mutation {
  registerAccount(input: IAccount): AccountType
}
